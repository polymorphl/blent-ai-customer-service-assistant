[project]
name = "blent-ai-customer-service-assistant"
version = "0.1.0"
description = "LangChain-based chatbot that answers e-commerce order questions in natural language by querying a SQL database in real time."
readme = "README.md"
requires-python = ">=3.14"
dependencies = []
